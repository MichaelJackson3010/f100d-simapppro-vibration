-- SimAppPro detection stub for the F-100D.
-- The module's files live under CoreMods\aircraft\F-100D, which SimAppPro's
-- module scan does not read - this stub makes the aircraft visible to it.
-- DCS executes nothing from this file: the block below is inside a Lua block
-- comment and exists only so SimAppPro's text scan can find the skins folder.
--[[
Skins	=
{
	{
	    name	= "F-100D",
		dir		= "Theme"
	},
}
]]
